# 测试计划

## 测试目标
验证公司管理平台所有 API 端点和前端数据展示功能。

## 测试用例

### 1. 健康检查
- [ ] 1.1 GET /api/health 返回状态码 200
- [ ] 1.2 返回数据包含 version 和 status 字段
- [ ] 1.3 status 值为 "ok"

### 2. 总览聚合 API
- [ ] 2.1 GET /api/dashboard 返回状态码 200
- [ ] 2.2 返回数据包含 kpi 对象
- [ ] 2.3 kpi 包含 totalProjects、inProgress、completed、pending 字段
- [ ] 2.4 kpi 包含 totalTasks、completedTasks、inProgressTasks、pendingTasks 字段
- [ ] 2.5 返回数据包含 departments 数组（2 个部门）
- [ ] 2.6 返回数据包含 projects 数组
- [ ] 2.7 返回数据包含 tasks 数组
- [ ] 2.8 返回数据包含 activities 数组
- [ ] 2.9 departments 包含 head 和 projects 字段
- [ ] 2.10 projects 包含 cycles 和 workflow 字段

### 3. 部门 API
- [ ] 3.1 GET /api/departments 返回状态码 200
- [ ] 3.2 返回数组包含 2 个部门
- [ ] 3.3 每个部门包含 id、name、color、member_count 字段
- [ ] 3.4 技术部 ID 为 "tech"，设计部 ID 为 "design"
- [ ] 3.5 每个部门包含 head（负责人）对象
- [ ] 3.6 负责人包含 id、name、role、avatar 字段
- [ ] 3.7 部门包含 short_name 和 color_hex 字段

### 4. 智能体 API
- [ ] 4.1 GET /api/agents 返回状态码 200
- [ ] 4.2 返回数组包含 4 个智能体
- [ ] 4.3 每个智能体包含 id、name、role、department 字段
- [ ] 4.4 Irra 在技术部，Mery 在设计部
- [ ] 4.5 每个智能体包含 abilities 数组
- [ ] 4.6 每个智能体包含 story 对象（summary 和 full）
- [ ] 4.7 每个智能体包含 avatar 和 colorTheme 字段

### 5. 项目 API
- [ ] 5.1 GET /api/projects 返回状态码 200
- [ ] 5.2 返回数组包含 5 个项目
- [ ] 5.3 每个项目包含 id、name、status、description 字段
- [ ] 5.4 每个项目包含 startDate、updatedAt 字段
- [ ] 5.5 每个项目包含 lead（负责人）字段
- [ ] 5.6 每个项目包含 involvedDepartments 数组
- [ ] 5.7 每个项目包含 cycles 和 workflow 字段
- [ ] 5.8 GET /api/projects/:id 返回单个项目详情
- [ ] 5.9 项目包含 completedTasks 和 taskCount 字段
- [ ] 5.10 状态值在有效范围内（planning、in-progress、completed、review）

### 6. 任务 API
- [ ] 6.1 GET /api/tasks 返回状态码 200
- [ ] 6.2 返回数组包含 26 个任务
- [ ] 6.3 每个任务包含 id、title、status、priority 字段
- [ ] 6.4 每个任务包含 projectId 和 assigneeId 字段
- [ ] 6.5 每个任务包含 departmentId 字段
- [ ] 6.6 GET /api/tasks/:id 返回单个任务详情
- [ ] 6.7 任务包含 dueDate 和 updatedAt 字段
- [ ] 6.8 任务状态值在有效范围内（todo、in-progress、completed、review）
- [ ] 6.9 任务优先级值在有效范围内（high、medium、low）
- [ ] 6.10 至少包含一个技术部任务和一个设计部任务

### 7. 文档 API
- [ ] 7.1 GET /api/documents 返回状态码 200
- [ ] 7.2 返回数组包含 10 个文档
- [ ] 7.3 每个文档包含 id、name、type、status 字段
- [ ] 7.4 每个文档包含 departmentId 和 updatedBy 字段
- [ ] 7.5 文档类型值在有效范围内（technical、design、product、meeting、architecture）
- [ ] 7.6 文档状态值在有效范围内（draft、review、published、archived）
- [ ] 7.7 每个文档包含 updatedAt 字段
- [ ] 7.8 至少包含一个技术部文档和一个设计部文档

### 8. 动态 API
- [ ] 8.1 GET /api/activities 返回状态码 200
- [ ] 8.2 返回数组包含 8 条动态记录
- [ ] 8.3 每条动态包含 id、type、action、target 字段
- [ ] 8.4 每条动态包含 userId、userName、userAvatar 字段
- [ ] 8.5 每条动态包含 createdAt 字段
- [ ] 8.6 动态类型值在有效范围内（project、task、document、agent、department）
- [ ] 8.7 至少包含一个项目动态和一个任务动态
- [ ] 8.8 动态记录按时间倒序排列

### 9. 前端页面检查
- [ ] 9.1 首页 / 返回状态码 200
- [ ] 9.2 页面标题为 "启明科技·管理驾驶舱"
- [ ] 9.3 前端包含 JS 和 CSS 资源文件

### 10. HTTPS 验证
- [ ] 10.1 HTTPS 访问正常
- [ ] 10.2 SSL 证书有效
- [ ] 10.3 DNS 解析正确（A 记录指向 Cloudflare）
- [ ] 10.4 通过 Cloudflare 边缘节点访问（location: sjcxx）

## 测试通过标准
- 所有 API 返回 HTTP 200 状态码
- 所有字段类型和数据格式正确
- 数据量与预期一致
- 关联数据正确（部门、项目、任务之间的关系）
- 前端页面正常渲染
- HTTPS 访问正常

## 测试输出
- 测试报告文件：/root/.openclaw/workspace/tech/projects/company-platform/04-测试/测试报告.md
- 测试日志：/tmp/company-platform-test.log
